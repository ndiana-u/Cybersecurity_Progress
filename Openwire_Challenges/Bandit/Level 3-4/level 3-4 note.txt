# LOGIN
ssh bandit3@bandit.labs.overthewire.org -p 2220

# DESCRIPTION
The password for the next level is stored in a hidden file in the inhere directory.

# STEPS
- change directory to inhere 'cd inhere'
- List all files 'ls -a'
-or find to get the name of the file
-To open the file 'cat ...Hiding-From-You'

### **Steps I Took**  

1. Changed into the **inhere** directory:  
   ```sh
   cd inhere
   ```  
2. Listed all files, including hidden ones:  
   ```sh
   ls -a
   ```  
3. Couldn’t spot the file immediately, so I used `find` to locate it.  
4. Once I found the file, I opened it with:  
   ```sh
   cat ...Hiding-From-You
   ```  

Some files like to play hide and seek, but `ls -a` and `find` always help!

# LEVEL 4 PASSWORD
2WmrDFRmJIq3IPxneAaMGhap0pFhF3NJ

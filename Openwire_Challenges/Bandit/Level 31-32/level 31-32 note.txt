LOGIN
ssh bandit31@bandit.labs.overthewire.org -p 2220


DESCRIPTION
There is a git repository at ssh://bandit31-git@localhost/home/bandit31-git/repo via the port 2220. The password for the user bandit31-git is
 the same as for the user bandit31.
Clone the repository and find the password for the next level.

COMMANDS YOU MAY NEED:
git


# STEPS I TOOK
1. Logged into bandit31 and set up a temporary directory with "mktemp -d". Moved into it and cloned the Git repository with:  
git clone ssh://bandit31-git@localhost:2220/home/bandit31-git/repo 

2. Used the same password as bandit31, then navigated into the repo and listed its contents. The "README.md" contained instructions: create a 
file named key.txt with the content "May I come in?" and push it to the master branch.  

3. Created the file:  
 echo 'May I come in?' > key.txt  

4. Checked the directory, and the file was there, but there was a .gitignore file. Opened it and saw it was set to ignore all .txt files. That was a 
problem since Git wouldn’t track key.txt.  

5. To fix this, deleted ".gitignore":  
 rm .gitignore 

6. Then staged, committed, and pushed the file:  
 git add .
 git commit -m 'Added key.txt as required'
 git push origin master 

7. Entered the password again, and just like that, the level 32 password appeared.  

# SKILLS LEARNT  
- Git Basics – Cloning repositories, checking contents, and working with branches.  
- Handling .gitignore – Understanding how ignored files affect Git tracking and how to bypass it.  
- Staging & Committing – Adding, committing, and pushing files to a remote repository.  
- Troubleshooting Git Issues – Identifying and resolving conflicts with ignored files.  

LEVEL 32 PASSWORD
3O9RfhqyAlVBEZpVb6LYStshZoqoSx5K

LOGIN DETAILS
Username: natas12
password: yZdkjAYZRd3R7tq7T5kXMjMJlOIkzDeB
URL:      http://natas12.natas.labs.overthewire.org


STEPS TO SOLVE IT
- First I went throught the source code to understand what it did and found a file upload vulnerability.
- I had to upload a php script.
- I intercepted the upload file request using Burp proxy and changed the .jpg extension of the new file name to .php so the script could be 
executed.
- I clicked on the file and I got the password.


LEVEL 13 PASSWORD
trbs5pCjCrkuSknBBKHhaBxq6Wm1j3LC


This code snippet is part of a file upload system that generates random file names and stores uploaded files in a specified directory. Here's a breakdown of how the code works:

Key Functions:
genRandomString():

This function generates a random string of 10 characters. It uses numbers (0-9) and lowercase letters (a-z) to form the string.
It randomly picks characters from the characters string by using mt_rand() within a loop until the string has 10 characters.
makeRandomPath($dir, $ext):

This function generates a random file path with the specified extension ($ext) in the given directory ($dir).
It keeps generating random file names (using genRandomString()) and checks if the file already exists. If the file doesn't exist, it returns the unique file path.
This ensures that each uploaded file has a unique name and doesn't overwrite existing files.
makeRandomPathFromFilename($dir, $fn):

This function takes a directory ($dir) and a file name ($fn). It extracts the file extension using pathinfo() and then calls makeRandomPath() to generate a random file path with the same extension.
File Upload Logic:
File Upload Handling:

When a user submits the form, it checks for the presence of a filename in the POST request.
If filename is provided, the script generates a random file path using makeRandomPathFromFilename("upload", $_POST["filename"]), where "upload" is the directory to store the uploaded files.
File Size Validation:

The script checks if the uploaded file size is greater than 1000 bytes (1 KB). If the file is too large, it outputs "File is too big" and halts further execution.
File Upload:

If the file size is acceptable, the script attempts to move the uploaded file from its temporary location ($_FILES['uploadedfile']['tmp_name']) to the target path ($target_path).
If successful, it displays a success message with a link to the uploaded file. If there's an error during the upload, it shows an error message.
HTML Form:
The form allows the user to select a file to upload. It uses enctype="multipart/form-data" to handle file uploads.
The filename input field is hidden and auto-generated by calling genRandomString(), giving the uploaded file a random name with a .jpg extension.
The MAX_FILE_SIZE hidden field ensures that the file uploaded is limited to 1 KB, but this is just an advisory limit—it's still validated on the server-side as well.

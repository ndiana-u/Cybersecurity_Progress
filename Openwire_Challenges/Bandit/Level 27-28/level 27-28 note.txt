LOGIN
ssh bandit27@bandit.labs.overthewire.org -p 2220


DESCRIPTION
There is a git repository at ssh://bandit27-git@localhost/home/bandit27-git/repo via the port 2220. The password for the user bandit27-git is 
the same as for the user bandit27.
Clone the repository and find the password for the next level.

COMMANDS YOU MAY NEED:
git

STEPS
1. log in to bandit27
2. mktemp -d : to create a temporary directory
3. cd /tmp/'tmp-directory': to change to that temporary directory
4. git clone ssh://bandit27-git@localhost:2220/home/bandit27-git/repo : to clone the repository
5. Input the password which is the same as bandit27 password
6. cd repo : change to the repo directory
7. ls -l : to list files/directories
8.  cat README : to read the content of the file.

Got it! This level was about **accessing a Git repository and retrieving hidden credentials**—a useful skill in **source code auditing and security assessments**.  

---

### **STEPS**  

1️⃣ **Logged into bandit27.**  

2️⃣ **Created a temporary directory to work in (keeps things clean):**  
   ```bash
   mktemp -d
   cd /tmp/'tmp-directory'
   ```  

3️⃣ **Cloned the Git repository using SSH:**  
   ```bash
   git clone ssh://bandit27-git@localhost:2220/home/bandit27-git/repo
   ```  
   - Entered the **bandit27 password** when prompted.  
   - The repository was successfully downloaded.  

4️⃣ **Navigated into the cloned repo and listed its contents:**  
   ```bash
   cd repo
   ls -l
   ```  

5️⃣ **Read the README file, which contained the password for bandit28:**  
   ```bash
   cat README
   ```  

---

### **Key Takeaways**  
✅ **Cloning a Git repository using SSH.**  
✅ **Navigating Git repositories and extracting useful information.**  
✅ **Understanding how sensitive data can sometimes be stored in version control.** 🚀

LEVEL 28 PASSWORD
Yz9IpL0sBcCeuG7m9uQFt8ZNpS4HZRcN





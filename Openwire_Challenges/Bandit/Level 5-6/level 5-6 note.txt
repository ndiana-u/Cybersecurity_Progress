# LOGIN
ssh bandit5@bandit.labs.overthewire.org -p 2220

# DESCRIPTION
The password for the next level is stored in a file somewhere under the inhere directory and has all of the following properties:
human-readable
1033 bytes in size
not executable

# COMMANDS YOU MIGHT NEED: 
ls , cd , cat , file , du , find


 
 STEPS I TOOK 
1. Listed the files and directories to see what was inside:  
   ls

2. Changed into the "inhere" directory:  
   cd inhere
  
3. Tried to check file sizes in bytes for all files:  
   du -b -a
  
4. Used "find" to locate the file with "1033 bytes":  
   find . -size 1033c
 
5. Once I found the file, I opened it with:  
   cat /.maybehere07/.file2

# MY STRUGGLES AND WHAT I LEARNED  
- First tried "du -h" for a readable format, but it only showed kilobytes—not exact byte sizes.  
- Then used "du -b", but the values were too large and not what I needed.  
- Finally, "du -b -a" worked since it listed each file and their sizes, including hidden ones in directories.  
- Realized that "maybehere" directories had more files inside, and "-a" helped display all of them.  
- Once I found the file with "1033 bytes", I just "cat"-ed it to see the content.  

Figuring out file sizes took a bit of trial and error, but "du -b -a" + "find" saved the day!


# LEVEL 6 PASSWORD
HWasnPhtq9AVKe0dmk45nxy20cvUa6EG
